import React from "react";
import {connect} from "react-redux";

class UnlockUserContainer extends React.Component {
    render() {
        return <div></div>;
    }
}

const mapStateToProps = state => {
    return {}
};

export default connect(mapStateToProps, null)(UnlockUserContainer)